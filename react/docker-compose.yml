services:
  react:
    image: node:22 #LTS（Long Time Support）の一番古いやつが22
    working_dir: /app
    volumes:
      - ./app:/app
    ports:
      - "5173:5173" #viteのデフォルトポートが5173らしい
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"